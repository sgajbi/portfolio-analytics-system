# pyproject.toml
[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "portfolio-analytics-system"
version = "0.1.0"
description = "Event-driven system for portfolio analytics."
readme = "README.md"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
exclude = ["tests*", "deployment*"]

# --- UPDATED: Pytest Configuration for PYTHONPATH and addopts ---
[tool.pytest.ini_options]
# Explicitly tell pytest where to find tests
testpaths = [
    "tests",
    "libs",
    "services"
]
pythonpath = [
    ".",
    "libs/portfolio-common",
    "libs/financial-calculator-engine", # Add the root for src imports
    "services/ingestion-service",
    "services/persistence_service",
    "services/query-service",
    "services/calculators/cashflow_calculator_service",
    "services/calculators/cost_calculator_service",
    "services/calculators/position_calculator",
    "services/calculators/position-valuation-calculator",
    "services/timeseries-generator-service",
    "tools"
]
addopts = "--strict-markers"
minversion = "6.0"
# Add registration for the asyncio marker
markers = [
    "asyncio: mark a test as being an asyncio test",
]
filterwarnings = [
    "ignore:Please use `import python_multipart` instead.:PendingDeprecationWarning"
]