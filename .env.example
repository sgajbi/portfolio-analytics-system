# This file provides environment variables for local development.
# Copy this to .env and modify as needed. DO NOT commit .env to version control.

# --- Kafka ---
# Use this address when running services locally on the host machine
KAFKA_BOOTSTRAP_SERVERS=localhost:9092

# --- PostgreSQL ---
# These are used by Alembic when running migrations from the host machine
POSTGRES_DB=portfolio_db
POSTGRES_USER=user
POSTGRES_PASSWORD=password
HOST_DATABASE_URL=postgresql://user:password@localhost:5432/portfolio_db